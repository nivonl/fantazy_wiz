[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Single-page app: serve index.html for any path (no client-side router yet, but this avoids
# 404s if one gets added later).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
