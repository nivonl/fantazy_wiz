# Where the FastAPI backend is reachable. Local dev: localhost. Netlify: set this in
# Site settings -> Environment variables to your deployed backend's public URL
# (Railway/Render/Fly.io), e.g. https://pitchmetric-api.up.railway.app
VITE_API_BASE_URL=http://localhost:8000
