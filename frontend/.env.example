# Where the FastAPI backend is reachable. Local dev: localhost. On Railway: set this as a
# Variable on the frontend service to the backend service's public URL,
# e.g. https://pitchmetric-api.up.railway.app
VITE_API_BASE_URL=http://localhost:8000
